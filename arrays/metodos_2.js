const numeros = [1, 2, 3, 4, 5, 8];

numeros.splice(1, 2); //remove elementos
console.log(numeros);

numeros.pop(); //remove o ultimo elemento
console.log(numeros);

console.log(numeros.pop()); //exibe qual elemento está sendo removido
console.log(numeros);
